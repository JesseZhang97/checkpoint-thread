interface:
  display_name: "Checkpoint Thread"
  short_description: "Observe, checkpoint, and ship shared Git work"
  default_prompt: "Use $checkpoint-thread to observe this task's repository edits without blocking normal tools, preserve recovery points, and organize verified business changes into commits."
