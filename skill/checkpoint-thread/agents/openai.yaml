interface:
  display_name: "Checkpoint Thread"
  short_description: "Auto-checkpoint and ship thread-owned Git work"
  default_prompt: "Use $checkpoint-thread to guard this task's repository mutations, preserve exact recovery points, and ship only verified thread-owned commits."
